name: Simple CI

on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
        - name: Checkout code
          uses: actions/checkout@v4

        - name: install dependecies
          run: npm install
        - name: Run tests 
          run: npm test
